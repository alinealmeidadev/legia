# FastAPI e servidor
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9
asyncpg==0.29.0

# Validação e serialização
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# Autenticação e segurança
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
pyotp==2.9.0

# Cache e queue
redis==5.0.1
celery==5.3.6

# HTTP Client
httpx==0.26.0

# Google Gemini/ADK
google-generativeai>=0.8.0

# Utilidades
python-dotenv==1.0.0
python-dateutil==2.8.2
pytz==2023.3

# Logging e monitoring
loguru==0.7.2

# REMOVIDOS TEMPORARIAMENTE (causam erro no Render free):
# aiohttp==3.9.1 (requer compilação C)
# python-magic==0.4.27 (requer libmagic do sistema)
# Pillow==10.2.0 (requer compilação pesada)
# pytest/faker (não precisamos em produção)
